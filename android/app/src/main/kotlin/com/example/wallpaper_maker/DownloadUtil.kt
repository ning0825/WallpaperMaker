package com.example.wallpaper_maker

import android.app.DownloadManager
import android.net.Uri

class DownloadUtil(uri: String) {
    var mUri = uri
    fun download(){
//        android.app.DownloadManager.Request request = android.app.DownloadManager.Request(Uri.parse(mUri));
    }
}